declare type Analysis = {
    consumed: string;
    parsed: number;
    rest: string;
};
declare type formatType = "숫자" | "숫자혼용" | "한자어" | "순우리말";
declare function extractNumber(word: string, format?: formatType[]): [number, string];
declare type Mapper<T> = (analysis: Analysis) => T | null;
declare function extractAndProcessNumber<T>(word: string, mapper: Mapper<T>, format?: formatType[]): T | null;
export { extractNumber, Analysis, formatType, extractAndProcessNumber, Mapper };
